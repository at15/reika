/* Examples for testing */

// x/; // NOTE: this commented out, I don't know why it's in the original test
x;

lambda x. x;
(lambda x. x) (lambda x. x x);

/* added by @at15 for https://github.com/at15/reika/issues/7 */
lambda x. x x;